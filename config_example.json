{
  "token": "bot token",
  "secret": "secret for github webhook",
  "guildId": "guild (server) id",
  "githubApiToken": "access token for creating issues",
  "dev": "true/false",
  "roles": {
    "adminRole": "admin role id",
    "modRole": "mod role id",
    "memberRole": "member role id",
    "updatePing": "update ping role id"
  },
  "wpmRoles": [
    {
      "id": "role id",
      "min": 0,
      "max": 99
    }
  ],
  "channels": {
    "botLog": "channel id where the bot will log debug messages",
    "general": "general id",
    "updates": "updates channel id"
  },
  "DB_URI": "mongodb://localhost:27017",
  "DB_NAME": "monkeytype"
}
